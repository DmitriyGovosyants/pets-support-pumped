import { RegisterForm, FormContainer } from 'components';

const RegisterPage = () => {
  return (
    <FormContainer>
      <RegisterForm />
    </FormContainer>
  );
};

export default RegisterPage;
